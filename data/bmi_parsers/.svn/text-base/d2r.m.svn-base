% convert bmi param from a diameter to a radius.
% assumes that the first column is the value and the second column is time
% (second column is of course unchanged)

function x = d2r(x)
	x(:,1)  = x(:,1) ./ 2;
end
